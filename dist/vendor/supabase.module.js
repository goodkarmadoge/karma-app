// supabase-js 2.58.0 (MIT, see SUPABASE-LICENSE.txt). Upstream bundles only a UMD
// build; imported for its side effect, which in module scope falls through to
// assigning self.supabase.
import './supabase.umd.min.js';
export const {createClient}=globalThis.supabase;
